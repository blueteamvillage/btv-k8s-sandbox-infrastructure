# Converged Frontier — s05 pro CHALLENGE (The Frosted Loop)
#
# Contestant-facing evidence pod. Inspect the artifacts:
#   kubectl -n converged-frontier exec -it challenge-001-s005-pro -- sh
#   #  then: ls /challenge/ ; cat /challenge/README.md ; ls -R /challenge/evidence/
#
# Published package: ghcr.io/blueteamvillage/challenge-001-s005-pro:latest
# For local dc34 testing, build/build.sh tags the same ref so IfNotPresent uses it.
# Inert, restricted-PSS compliant (no PolicyException needed).
apiVersion: v1
kind: Pod
metadata:
  name: challenge-001-s005-pro
  namespace: converged-frontier
  labels:
    app: converged-frontier
    scenario: s05
    track: pro
    kind: challenge
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: challenge
      image: ghcr.io/blueteamvillage/challenge-001-s005-pro:latest
      # IfNotPresent uses the image built into the dc34 node by build/build.sh.
      imagePullPolicy: IfNotPresent
      env:
        - name: SCENARIO_TITLE
          value: "The Frosted Loop"
        - name: SCENARIO_TRACK
          value: pro
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      resources:
        requests:
          cpu: 50m
          memory: 32Mi
        limits:
          cpu: 100m
          memory: 64Mi
