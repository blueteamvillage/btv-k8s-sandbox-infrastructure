# Converged Frontier — s01 beginner CHALLENGE (Cloud-to-OT Control Plane Compromise)
#
# Contestant-facing evidence pod. Inspect the artifacts:
#   kubectl -n converged-frontier exec -it challenge-001-s001-beginner -- sh
#   #  then: ls /challenge/ ; cat /challenge/README.md ; ls -R /challenge/evidence/
#
# Published package: ghcr.io/blueteamvillage/challenge-001-s001-beginner:latest
# For local dc34 testing, build/build.sh tags the same ref so IfNotPresent uses it.
# Inert, restricted-PSS compliant (no PolicyException needed).
apiVersion: v1
kind: Pod
metadata:
  name: challenge-001-s001-beginner
  namespace: converged-frontier
  labels:
    app: converged-frontier
    scenario: s01
    track: beginner
    kind: challenge
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: challenge
      image: ghcr.io/blueteamvillage/challenge-001-s001-beginner:latest
      # IfNotPresent uses the image built into the dc34 node by build/build.sh.
      imagePullPolicy: IfNotPresent
      env:
        - name: SCENARIO_TITLE
          value: "Cloud-to-OT Control Plane Compromise"
        - name: SCENARIO_TRACK
          value: beginner
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      resources:
        requests:
          cpu: 50m
          memory: 32Mi
        limits:
          cpu: 100m
          memory: 64Mi
